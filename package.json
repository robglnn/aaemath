{
  "name": "variable-star",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "description": "Variable Star — an adaptive Algebra I mastery world in the browser.",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview --port 4180 --strictPort",
    "deploy": "vite build --outDir ../docs --emptyOutDir && node -e \"require('fs').writeFileSync('docs/.nojekyll','')\"",
    "bank-audit": "node tools/bank-audit.mjs",
    "bank-audit:check": "node tools/bank-audit.mjs --check",
    "review": "node tools/review.mjs",
    "shot": "node tools/review.mjs shot",
    "verify": "node tools/review.mjs verify"
  },
  "dependencies": {
    "katex": "^0.16.11",
    "three": "^0.169.0"
  },
  "devDependencies": {
    "playwright": "^1.62.1",
    "vite": "^5.4.0"
  }
}
