
P31 — per-lesson item loading. Measured against a real vite build, not a description of one.
====================================================================================================
PASS  A1  the eager item-bank chunk shrank on the real build (npm run build, gzipped at vite's own level)
        measured : ItemBank-B5q9etEn.js 1616.3 kB / 147.0 kB gz  ->  ItemBank-Bkv3MSq0.js 121.5 kB / 29.2 kB gz  (-117.8 kB gz)
        threshold: > 60 kB gzipped removed from the eager chunk

PASS  A2  FIRST LOAD — every chunk the page pulls before the first item, one locale, gzipped
        measured : 548.0 kB gz -> 454.6 kB gz  (-93.5 kB gz, -17.1%). Raw 2974.7 kB -> 1556.2 kB.
        threshold: > 55 kB gzipped off first load
        note     : The after-tree carries 2 chunks that did not exist at the baseline commit (02-autotier, 90-flow), costing 13.3 kB gz — other pieces building in parallel. They ADD to the after number; the reduction is measured against that handicap.
        note     : Like for like — first load minus those new chunks — is 441.3 kB gz, a reduction of 106.8 kB gz.

PASS  A3  the catalogue built as one chunk per knowledge point, none of them on the first-load path
        measured : 32 group chunks totalling 907.9 kB / 93.0 kB gz, mean 2.9 kB gz each
        threshold: 32 chunks

PASS  A4  a single-lesson session's catalogue cost, measured on the SHIPPED chunks
        measured : 22 lessons, 2342–7895 B gz (median 3438 B), each 10–22 min of scored-item time — against 93.0 kB gz for the whole course
        threshold: every lesson under 12 kB gzipped

PASS  A5  the REAL built game reaches `ready` having requested ZERO item groups, ZERO spine and ONE locale — and still knows the whole course
        measured : at ready the page had made 30 requests (583.1 kB encoded): 0 group chunks, 0 spine chunks, item locale ["items-en-BvtiMYOY.js"]; the bank still reports 32 knowledge points, 1152 catalogue items, min 36/kp, 22 lessons
        threshold: 0 group chunks, 0 spine chunks, exactly 1 item locale chunk on the critical path

PASS  B1  a 25-minute session runs end to end from a COLD cache, opened through the BOOT MODULE's own warm, and never serves a blank item
        measured : warmFrontier() -> frontier "var-meaning" -> lesson "expressions-1" (3 groups, 0 failed, reason "ok"); then 36 items served over 5 knowledge points, 0 blanks, 37 scheduler steps (the scheduler closed the session on its own 25-minute budget, not on a bound in this script)
        threshold: >= 25 items, 0 blanks, terminates, and the lesson came from the engine's frontier

PASS  B2  the session pulled ONLY the groups it needed — not the course
        measured : 5/32 groups resident (var-meaning, oo-numeric, expr-anatomy, oo-structure, props-operations); 13.2 kB gz of shipped catalogue for the whole session, against 93.0 kB gz for all thirty-two
        threshold: groups touched + at most 4 prefetched, and strictly fewer than 32

PASS  B3  every item the session served came back checkable and on the right knowledge point
        measured : 35 from the catalogue, 1 generated, 25 marked correct by the shipped checker
        threshold: every item identified and marked

PASS  B4  the NEXT likely group is prefetched during idle — offline AND in the real built game
        measured : offline: warming expressions-1 loaded 3 groups, then idle prefetch warmed 1 more (oo-structure). live built game: after the warm resolved, two idle turns pulled 1 further group chunk(s) (oo-structure-Bges-2rk.js, 2619 B encoded)
        threshold: at least one group ahead, in both

PASS  C1  a group that never arrives degrades to a playable session AND says so — no hang, no blank item
        measured : group "oo-numeric" made to fail (it carried 8 of the clean run's 36 items): 36 items still served, 0 blanks, 8 of them on the failed knowledge point, all generated: true; probe().degraded = ["oo-numeric: simulated transport failure loading group \"oo-numeric\" (3 attempts, retry in 29s)"]
        threshold: session completes, >=1 item still served on the failed knowledge point, 0 blanks, the failure named in the probe

PASS  C3  a group that failed ONCE is retried after the backoff window instead of being degraded for the life of the page
        measured : "eq-two-step" made to fail: 3 attempts inside one load (250 ms then 750 ms apart), selects during the outage relaxed "generated-group-failed"; after the 30s window the next select still answered "generated-group-failed" synchronously but started a retry (issues: group-load-failed, group-retry, group-recovered), and the following select came from the catalogue
        threshold: 3 attempts, retried after the window, back on the catalogue, and the retry announced

PASS  C2  a cold `select()` answers synchronously with a real, checkable, correctly-tagged item and starts the load
        measured : cold -> generated/generated-group-absent (ineq-negative-flip.subneg/construct/14xp4di), its own correct answer marks true; after one tick the same request -> catalogue/exact
        threshold: generated-group-absent, checkable, then catalogue

PASS  D1  the catalogue fingerprint is UNCHANGED by the split, so the committed pricing table still describes the bank it prices
        measured : computed d6e2537d, committed in bank-audit.json d6e2537d
        threshold: identical

PASS  D2  the eager spine carries every item's id, form, family and band, in the committed order (P17 C18 shape)
        measured : 32 files, 1152 items, ids identical, forms/families/bands identical
        threshold: identical

PASS  D3  every item served from a group chunk is field-for-field the item in content/items/bank/*.json
        measured : 1152 items compared, 0 differ (English "text" snapshots excluded — the game never renders them)
        threshold: 0 differ

PASS  D4  the split changed DELIVERY and nothing else: 5,088 seeded selections come back byte-identical to the pre-split tree
        measured : digest 567d7e50 over 5088 selections (4985 catalogue / 103 generated / 0 null); pre-split tree at 9d7cc64566 gave 567d7e50
        threshold: identical digest

PASS  D5  the manifest is complete, single-valued and Pomodoro-shaped, and the shipped copy equals the readable one
        measured : manifest.mjs == manifest.json: true; 22 lessons cover 32/32 knowledge points, 0 orphans, 0 in two lessons, 0 over 25 min, item counts agree: true
        threshold: 0 orphans, 0 doubled, 0 over-long, counts agree

PASS  D6  the coverage numbers a reviewer reads do not depend on what happens to be loaded
        measured : with 0 groups resident: 32 kps, min 36 items/kp, min 3 items/misconception, 1152 catalogue items — identical with all 32 resident: true
        threshold: identical cold and warm

PASS  D7  the item locale table is one chunk per language and the three reassemble to strings.json exactly — and the page pulls ONE
        measured : 281 keys x 3 locales; 0 strings differ from strings.json, 0 English leaks through text() in es/pl; the reassembled table is byte-identical to strings.json; the live built game pulled ["items-en-BvtiMYOY.js"]
        threshold: 0 differences, 0 leaks, exactly 1 locale chunk fetched

PASS  E1  the per-lesson loader is CALLED from the shipped game — executable code, not a doc comment — and the degradation channel has a subscriber
        measured : with comments stripped, `warmFrontier`/`warmFrontierWhenIdle` is CALLED in [app/src/boot/62-itembank.js], `ensure`/`ensureLesson` in [NOTHING], and `bankIssues.onIssue` is assigned in [app/src/boot/62-itembank.js]. Driving that entry point once reached ensureLesson x1, ensure x1, prefetchAround x1 and opened "expressions-1"
        threshold: a boot module calls warmFrontier; the call reaches ensureLesson and prefetchAround; onIssue has a subscriber

PASS  E2  in the REAL built game the warm ran, off the critical path, on the lesson the ENGINE named — and pulled that lesson's chunks
        measured : learning.frontier() = ["var-meaning","oo-numeric"]; the warm resolved lesson "expressions-1" from "var-meaning" in 7.1 ms and loaded 3 groups. The browser's resource timings show those exact 3 chunks (var-meaning-Ct12kn0-.js, oo-numeric-BjsVHueV.js, expr-anatomy-WYJhDm_0.js, 7895 B encoded) requested AFTER ready, none before it.
        threshold: warm ok, frontier-derived lesson, >0 group chunks, all after ready

PASS  E3  the audit fingerprint ships as a SCALAR, matches a full recomputation, and the identity spine is never requested by the page
        measured : BANK_FINGERPRINT d6e2537d == recomputed over all 1152 items d6e2537d == committed d6e2537d; basis key e5a9c7e1 recomputed from the live Mastery constants gives e5a9c7e1; the live game reports fingerprintSource "build-time" and resolved the price in 0.1 ms, having requested 0 spine chunks
        threshold: identical, basis agrees, spine never fetched

----------------------------------------------------------------------------------------------------
catalogue chunk : 1616.3 kB / 147.0 kB gz  ->  121.5 kB / 29.2 kB gz
first load      : 548.0 kB gz  ->  454.6 kB gz   (-93.5 kB gz)
one lesson      : 2342–7895 B gz of catalogue (22 lessons, whole course 93.0 kB gz)
cold session    : 36 items, 5 groups pulled, 13.2 kB gz
----------------------------------------------------------------------------------------------------
22/22 claims pass — P31 PASSES

